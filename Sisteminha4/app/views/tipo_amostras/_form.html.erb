<%= form_for(@tipo_amostra) do |f| %>
  <% if @tipo_amostra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_amostra.errors.count, "erro proíbe") %> este tipo de amostra de ser salvo:</h2>

      <ul>
      <% @tipo_amostra.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
